{
  "name": "@oshimaryo/get-canvas-max-size",
  "version": "0.0.5",
  "description": "get the maximum size of the canvas element in the browser",
  "main": "./lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./esbuild.js",
    "prepublish": "npm run clean && npm run build && npm run build-licenses",
    "build-licenses": "license-checker --production --direct --customPath ./license-checker_customFormat.json --csv --out ./licenses.csv"
  },
  "files": [
    "lib",
    "src"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oshimaryo/get-canvas-max-size.git"
  },
  "keywords": [
    "canvas",
    "front-end"
  ],
  "author": "Ryo Oshima <oshimaryo@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/oshimaryo/get-canvas-max-size/issues"
  },
  "homepage": "https://github.com/oshimaryo/get-canvas-max-size#readme",
  "dependencies": {
    "ua-parser-js": "^1.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.10",
    "license-checker": "^25.0.1",
    "rimraf": "^2.6.2"
  }
}
